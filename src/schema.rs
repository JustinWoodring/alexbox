table! {
    tiles (id) {
        id -> Integer,
        title -> Text,
        command -> Text,
        day -> Integer,
        start_time -> Text,
        duration -> Float,
        color -> Integer,
    }
}
